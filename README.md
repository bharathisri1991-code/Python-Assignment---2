# Python Assignment 2 – Data Structures & Conditional Statements

##   Objective

This assignment demonstrates the use of Python's fundamental data structures and conditional statements. It covers List, Dictionary, Set operations, and decision-making using `if`, `elif`, and `else`.

---

##   Topics Covered

- Lists
- Dictionaries
- Sets
- Conditional Statements (`if`, `elif`, `else`)

---

##   Tasks Completed

## 1. List Operations
- Created `age_list` and `name_list`
- Appended and inserted elements
- Removed and popped elements
- Extended the list
- Sorted the list in descending order
- Found maximum, minimum, and sum of elements
- Accessed list elements using indexing and slicing

### 2. Dictionary Operations
- Created a dictionary of student names and marks
- Accessed a student's mark
- Added a new student
- Updated an existing student's mark
- Displayed all keys, values, and key-value pairs

### 3. Set Operations
- Created a set with duplicate values
- Observed automatic removal of duplicates
- Understood why sets do not support indexing
- Performed union and intersection operations

### 4. Conditional Statements
- Accepted a score from the user
- Classified performance using:
  - Above Average
  - Average
  - Below Average
- Displayed appropriate feedback based on the score

---

##   Concepts Practiced

- List Methods
  - `append()`
  - `insert()`
  - `remove()`
  - `pop()`
  - `extend()`
  - `sort()`

- Dictionary Methods
  - `keys()`
  - `values()`
  - `items()`

- Set Operations
  - `add()`
  - `union()`
  - `intersection()`

- Conditional Statements
  - `if`
  - `elif`
  - `else`

---

##   Learning Outcomes

After completing this assignment, I learned to:

- Create and modify lists, dictionaries, and sets
- Access data using indexing and slicing
- Perform dictionary operations efficiently
- Understand the uniqueness property of sets
- Apply conditional statements for decision-making
- Write clean and beginner-friendly Python programs

##   Conclusion

This assignment strengthened my understanding of Python data structures and conditional statements. It provided practical experience in working with lists, dictionaries, sets, and decision-making logic, forming a strong foundation for advanced Python programming.

